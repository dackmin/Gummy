.get-infos(ng-controller="GetInfosCtrl")

    //# CLOSE BUTTON
    a.close(href="", ng-click="close()")
        i.s-times

    //# CONTENT, LOL
    .content
        h2 What we found for
        form(ng-submit="seek()")
            img(src="images/loaders/puff.svg", ng-if="loading.list == true")
            input(type="text", value="{{ movie.filename }}", placeholder="Type a movie name here", ng-model="filename")
            
            .errors(ng-if="errors.length > 0")
                i.s-bug
                | Ewwwww. Something went bad with Trakt API. Try again later.
        
        .infos.clearfix(ng-repeat="movie_infos in movies", ng-click="chose(movie_infos)")
            .thumb(style="background-image: url({{ movie_infos.cover.small }})")
            h3.title {{ movie_infos.title }}
            p.additional
                span {{ movie_infos.year }}
                span {{ movie_infos.rating }}
            p.synopsis {{ movie_infos.synopsis }}
